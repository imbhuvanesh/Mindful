import 'package:flutter/material.dart';

/// Mindful — black-and-white glass palette.
class MindfulColors {
  MindfulColors._();

  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  static const gray = Color(0xFF9A9A9A);
  static const mist = Color(0xFFD9D9D9);

  // Glass fills
  static const transparentGlass = Color(0x0F000000);
  static const glassFill = Color(0x14FFFFFF); // ~8% white
  static const glassBorder = Color(0x2EFFFFFF); // ~18% white
  static const glassStrong = Color(0x33FFFFFF); // ~20% white
}